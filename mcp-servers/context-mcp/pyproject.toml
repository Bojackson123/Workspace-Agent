[project]
name = "context-mcp"
version = "0.1.0"
description = "Context MCP Server — Read-only user context via Domain-Wide Delegation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.0",
    "google-api-python-client>=2.170.0",
    "google-auth-httplib2>=0.2.0",
    "google-auth-oauthlib>=1.2.0",
    "uvicorn[standard]>=0.34.0",
]
